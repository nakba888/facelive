# 📝 연구노트 (Research Note)

---

## 📌 1. 연구 과제 개요

| 항목 | 내용 |
| :--- | :--- |
| **과제명** | OpenCV YuNet 딥러닝 모델 기반 실시간 얼굴 검출 및 인터랙티브 카메라 제어 시스템 개발 |
| **연구 개발 기간** | 2026.07 ~ 2026.08 |
| **개발 언어 및 환경** | Python 3.12, OpenCV 5.0+, NumPy |
| **주요 적용 기술** | YuNet ONNX 딥러닝 모듈, 5-Landmark 검출, 면적 기반 정렬, 실시간 변환 파이프라인 |

---

## 🎯 2. 연구 개발 목적 및 필요성

1. **경량 딥러닝 모델 기반의 고속 얼굴 검출 기술 확보**
   - 기존 Haas-Cascade 또는 HOG 모델 대비 조명 변화 및 얼굴 회전 환경에서 우수한 검출률을 자랑하는 **YuNet ONNX** 딥러닝 모델을 채택하여 노트북/엣지 디바이스 환경에서 30+ FPS 이상의 고속 추론 성능 확보.
2. **다중 얼굴 크기 분석 및 시각적 직관성 제공**
   - 검출된 다수의 얼굴 객체 중 카메라와의 거리에 따른 우선순위(면적)를 산출하고, 무지개 범례(Rainbow Rank)를 적용하여 직관적인 분석 인터페이스 제공.
3. **인터랙티브 카메라 제어 및 이식성 극대화**
   - 실시간 해상도 변경, 가로/세로(9:16 모바일 뷰) 비율 변환, 내장/USB 외장 웹캠 동적 전환, 잔상 없는 멈춤(Freeze) 기능 및 타임스탬프 기반 데이터 저장 기능 통합 개발.

---

## ⚙️ 3. 주요 기술 구현 및 핵심 알고리즘

### 3.1. YuNet ONNX 딥러닝 모델 자동 동기화
- 파이썬 실행 시 무거운 모델 파일(`face_detection_yunet_2023mar.onnx`)이 로컬 환경에 존재하지 않을 경우, 원격 저장소로부터 **자동 다운로드 및 로드**하는 가용성 파이프라인 구현.

### 3.2. 얼굴 면적(Size Area) 기반 무지개 범례(Rainbow Ranking) 정렬 알고리즘
- 감지된 얼굴 바운딩 박스의 너비와 높이($w \times h$)를 기반으로 면적을 계산하고 내림차순 정렬.
- 가장 카메라와 가까운 1위 얼굴부터 **빨(Red) → 주(Orange) → 노(Yellow) → 초(Green) → 파(Blue) → 남(Indigo) → 보(Violet)** 무지개 색상과 순위 표식을 동적 매핑하여 라벨링 처리.

### 3.3. 잔상 방지 렌더링 파이프라인 (Clean Frame Double-Buffering)
- 화면 멈춤(Freeze/Pause) 시 이전 프레임의 OSD(On-Screen Display) 레이어가 중복되어 잔상이 남는 문제를 해결하기 위해, 원본 카메라인 `raw_frame`을 별도 격리 유지하고 시각화 레이어를 매 루프마다 새로 합성하는 더블 버퍼링 구조 설계.

### 3.4. 가로/세로(Portrait 9:16) 기하학적 변환 및 AI 크기 동기화
- `cv2.rotate` 및 9:16 세로 크롭 파이프라인 구축.
- 프레임 회전/크롭 발생 직후 `detector.setInputSize((new_w, new_h))`를 동적 호출하여 **세로로 변경된 프레임 위에서 딥러닝 검출이 100% 정밀하게 수행**되도록 동기화.

### 3.5. 데이터 저장 파이프라인
- `S` 키 입력 시 `captures/` 전용 디렉터리를 자동 생성하며, 파일명에 **년월일_시분초 타임스탬프(`capture_YYYYMMDD_HHMMSS_...png`)**를 부여하여 무손실 캡처 데이터 덮어쓰기 방지.

---

## 💻 4. 소프트웨어 환경 구축 및 가상환경 격리 (Environment Setup)

### 4.1. 파이썬 독립 가상환경(Virtual Environment) 세팅의 당위성
- 시스템 글로벌 파이썬 라이브러리와의 패키지 버전 충돌을 방지하고, 타 기기(노트북/서버) 재설치 시 동일한 런타임 환경을 재현하기 위해 `venv` 및 `Conda` 독립 환경 구조 적용.

### 4.2. 설치 절차 요약 (배포 절차)
1. **소프트웨어 런타임 준비**: Python 3.12+ 획득 및 환경변수(PATH) 등록
2. **독립 환경 구동**:
   - Conda 환경: `conda create -n facelive python=3.12 -y` → `conda activate facelive`
   - Python venv: `python -m venv facelive` → `.\facelive\Scripts\Activate.ps1`
3. **의존성 복원**: `pip install -r requirements.txt` (OpenCV, NumPy)

---

## ⌨️ 5. 시스템 제어 인터페이스 단축키 사양

| 단축키 | 시스템 기능 사양 |
| :---: | :--- |
| **`Space`** | 프레임 동결 / 실시간 재생 토글 (Freeze / Resume) |
| **`V`** | 가로 ↔ 세로 화면 방향 조절 (가로 16:9, 90° 시계/반시계 회전, 9:16 크롭) |
| **`1` ~ `4`** | 해상도 즉시 동기화 (`1`: 240p QVGA ~ `4`: 1080p FHD) |
| **`R`** | 해상도 순환 전환 |
| **`C`** | 카메라 장치 전환 (0: 노트북 내장웹캠 ↔ 1: USB 외장웹캠) |
| **`F`** | 실시간 FPS(Frames Per Second) 디스플레이 ON / OFF |
| **`S`** | `captures/` 폴더에 타임스탬프 이미지 저장 |
| **`Q / ESC`** | 시스템 정상 종료 및 리소스 해제 |

---

## 📊 6. 연구 결과 및 결론

1. **실시간 검출 성능 달성**: 720p HD 해상도 기준 평균 30 FPS 이상의 안정적 프레임 레이트 유지.
2. **이식성 검증 완료**: GitHub 저장소를 통한 런타임 의존성 관리로 타 노트북/PC 환경에서 3분 이내로 동일 시스템 구축 및 구동 검증 완료.
