# 📝 2주차 연구개발 보고서 (Week 2 Research Report)

**과제명**: OpenCV YuNet 딥러닝 기반 실시간 얼굴 검출 및 인터랙티브 제어 시스템  
**연구 기간**: 2주차 (2026.07.27 ~ 2026.07.31)  
**주제**: **인터랙티브 카메라 제어, 영상 변환 파이프라인 및 패키징 배포**  

---

## 🎯 1. 2주차 연구 개발 목표
- 더블 버퍼링 기반 Clean Frame 멈춤(Freeze) 기능 및 잔상 제거
- 실시간 해상도/화질 즉시 변경 및 드라이버 예외 안전성 확보
- 세로(Portrait 9:16) 화면 비율 변환 및 AI 입력 크기 동기화
- 멀티 카메라(내장/USB 외장) 지원, 타임스탬프 파일 저장 및 저장소 패키징

---

## 🔬 2. 핵심 연구 및 개발 성과

### 2.1. 잔상 없는 Clean Frame 더블 버퍼링 (Freeze Mode)
- `Space Bar` 입력 시 OSD 레이어 중복 텍스트 겹침 현상을 방지하기 위해 `raw_frame`과 `display_frame`을 분리하는 더블 버퍼링 구조 설계.

### 2.2. 실시간 해상도 조절 (`1` ~ `4`, `R` 키) 및 드라이버 예외 복구
- `1` (240p) ~ `4` (1080p FHD) 동적 해상도 변경 구현. Windows MSMF 드라이버 오류 예방을 위해 `cv2.CAP_DSHOW` 우선 적용 및 미지원 해상도 자동 백업 복구 로직 구현.

### 2.3. 세로(Portrait 9:16) 기하학적 변환 (`V` 키) 및 AI 크기 동기화
- `cv2.rotate` 및 9:16 세로 크롭 파이프라인 구축. 프레임 변환 즉시 `detector.setInputSize()`를 재호출하여 세로 화면에서도 딥러닝 탐지 좌표가 100% 정확하게 연동되도록 구현.

### 2.4. 멀티 카메라 지원 (`C` 키) 및 타임스탬프 데이터 저장 (`S` 키)
- 실행 중 `C` 키로 노트북 내장 웹캠(Cam #0) ↔ USB 외장 웹캠(USB Cam #1) 실시간 전환.
- `S` 키 입력 시 `captures/` 전용 폴더에 `capture_년월일_시분초_...png` 타임스탬프 파일명으로 무손실 보존 파이프라인 구축.

### 2.5. 종합 검증 및 GitHub 패키징 배포
- 저장소(`nakba888/facelive`) 공개 배포 및 교차 테스트 수행 완료. 타 컴퓨터 환경에서 3분 이내 원클릭 구축 검증 완료.

---

## 📊 3. 2주차 결론 및 성과 요약
- **시스템 완성도**: 딥러닝 탐지, 화질 조절, 세로모드, 카메라 스위칭, 타임스탬프 저장이 통합된 시스템 구축 완료
- **배포 가용성**: GitHub 동기화 및 런타임 독립성 확보로 실무 적용 가능 수준 검증
